goodwe>=0.4.0,<0.5    # pin major: avoid surprise discovery/API changes on rebuild
pymysql>=1.1.0
pymongo>=4.6.0
redis>=5.0.0
tzdata>=2024.1       # IANA timezone DB for zoneinfo (python:slim has none) — fixes app-timezone display
